import { AppShell } from '@/components/app-shell';

export default function BlogPage() {
  return (
    <AppShell>
      <h2>وبلاگ مدرسه</h2>
      <p>مشاهده برای همه، ایجاد مقاله فقط توسط ادمین.</p>
    </AppShell>
  );
}
